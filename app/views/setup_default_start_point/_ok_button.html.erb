
<script type="text/javascript"><!--

$(function() {

  $('#ok').click(function() {
    var params = {
             major: "<%= @language %>",
             minor: "<%= @test %>",
      instructions: $('[id^=instructions_name_][class~=selected]').data('name'),
    };
    $.getJSON('/setup_default_start_point/save', params, function(dojo) {
      cd.newDojoDialog(dojo.id);
    });
  });

});

//--></script>

<button type="button" id="ok">ok</button>
