
<div id="exercises-list">
  <% @exercises_names.each_with_index do |name, index| %>
    <div class='filename'
         id="exercises_name_<%= index %>"
         data-name="<%= name %>" >
      <%= name.gsub('_', ' ') %>
    </div>
  <% end %>
</div>