
<script type="text/javascript"><!--

$(function() {

  var gotoChooseExercisesPage = function() {
    var id = "<%= @id %>";
    var url = '/setup_default_start_point/show_exercises/' + id +
      '?language=' + encodeURIComponent(cd.chosenMajor()) +
      '&test=' + encodeURIComponent(cd.chosenMinor());
    window.location = url;
  };

  $('#next').click(function() {
    $.getJSON('/setup_default_start_point/pull_needed', cd.chosenMajorMinor(), function(dojo) {
        if (dojo.pull_needed) {
          cd.dialog_pullImageThen('/setup_default_start_point/pull', gotoChooseExercisesPage);
        } else {
          gotoChooseExercisesPage();
        }
    });
  });

});

//--></script>

<button type="button" id="next">next</button>
