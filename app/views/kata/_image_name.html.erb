<input name="image_name" type="hidden" value="">

<script>
'use strict';
$(() => {

  const $imageName = $("input[name='image_name']");
  const manifest = cd.kata.manifest(cd.kata.id);
  const imageName = manifest.image_name;

  $imageName.val(imageName);

});
</script>
