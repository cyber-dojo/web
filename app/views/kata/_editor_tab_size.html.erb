<script>
'use strict';
$(() => {

  cd.kata.editor.tabSize = () => {
    const manifest = cd.kata.manifest(cd.kata.id);
    console.log(JSON.stringify(manifest));
    return manifest.tab_size;
  };

});
</script>
