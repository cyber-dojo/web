<script>
'use strict';
$(() => {

  cd.showStartingInfoDialog = () => {
    if (cd.kata.index === 1 && cd.env.STARTING_INFO_DIALOG !== 'off') {
      cd.trafficLightsButton().click();
    }
  };

});
</script>
