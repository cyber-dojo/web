<input name="max_seconds"  type="hidden" value="">

<script>
'use strict';
$(() => {

  const $maxSeconds = $("input[name='max_seconds']");
  const manifest = cd.kata.manifest();
  $maxSeconds.val(manifest.max_seconds);

});
</script>
