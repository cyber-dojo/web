<input name="max_seconds"  type="hidden" value="">

<script>
'use strict';
$(() => {

  const $maxSeconds = $("input[name='max_seconds']");
  const manifest = cd.kata.manifest(cd.kata.id);
  const maxSeconds = manifest.max_seconds;

  $maxSeconds.val(maxSeconds);

});
</script>
