<script type="text/javascript"><!--
'use strict';
$(() => {

  cd.extensionFilenames = () => {
    // Used in app/views/review/show page/dialog
    // which is also loaded in app/views/kata/traffic_lights partial
    return $.parseJSON('<%= @kata.manifest.filename_extension.inspect.html_safe %>');
  };

});
//--></script>
