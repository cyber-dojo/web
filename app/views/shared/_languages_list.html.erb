
<div id="languages-list">
  <% @display_names.names.each_with_index do |name, index| %>
    <div class="filename"
         id="language_<%= index %>"
         data-language="<%= name %>"
         data-index="<%= index %>"
         data-test-index="<%= @display_names.tests_indexes[index][0] %>">
      <%= name %>
    </div>
  <% end %>
</div>