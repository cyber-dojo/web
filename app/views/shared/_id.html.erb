<div id="id-box"></div>

<script>
'use strict';
$(() => {

  const $idBox = $("#id-box");
  const manifest = cd.kata.manifest(cd.kata.id);
  const id = manifest.group_id || cd.kata.id;
  $idBox.html(id);

});
</script>
