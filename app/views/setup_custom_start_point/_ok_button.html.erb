
<script type="text/javascript"><!--

$(function() {

  var createDojo = function() {
    $.getJSON('/setup_custom_start_point/save', cd.chosenLanguageAndTest(), function(dojo) {
      cd.newDojoDialog(dojo.id);
    });
  };

  $('#ok').click(function() {
    $.getJSON('/setup_custom_start_point/pull_needed', cd.chosenLanguageAndTest(), function(dojo) {
      if (dojo.pull_needed) {
        cd.dialog_pullImageThen('/setup_custom_start_point/pull', createDojo);
      } else {
        createDojo();
      }
    });
  });

});

//--></script>

<button type="button" id="ok">ok</button>



