#-----Ignore everything
#-----including katas (data-container / volume-mounted)
#-----including public (nginx image)

*

#-----!Except these folders

!app/assets
!app/controllers
!app/helpers
!app/lib
!app/models
!app/views
!script
!config
!lib
!test

#-----!Except these _empty_ folders
!app/caches
!app/downloads
app/caches/*
app/downloads/*

#-----!Except these files

# {bundle install} in web's Dockerfile (inside Travis pipeline) needs these
!Gemfile
#!Gemfile.lock

# {rails server} in web's docker-compose.yml needs this
!config.ru

# {cyber-dojo update} copies these out of web container
!app/docker/cyber-dojo.sh
!app/docker/docker-compose.yml

# {cyber-dojo.sh} copies these out of web container
!app/docker/katas/*

# {cyber-dojo} delegates to cyber-dojo.sh which delegates to this inside web container
!app/docker/cyber-dojo.rb
