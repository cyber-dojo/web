version: 1

trail:
  attestations:
    - name: pull-request
      type: pull_request

  artifacts:
    - name: web
      attestations:
        - name: snyk-container-scan
          type: generic
        - name: provenance-attestation
          type: generic
        - name: sbom
          type: generic
