version: 1

trail:
  attestations:
    - name: pull-request
      type: pull_request

  # artifacts:
  #   - name: artifact
  #     attestations:
  #       - name: VULN-NAME
  #         type: aws-snyk-vuln-ages