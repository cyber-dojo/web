<script>
'use strict';
$(() => {

  cd.kata.scrollLastTrafficLightIntoView = () => {
    // called edit.html.erb on page load/refresh
    const last = document.querySelector('#kata-page .diff-traffic-light:last-of-type');
    last?.scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'nearest' });
  };

});
</script>
